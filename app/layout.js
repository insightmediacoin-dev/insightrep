import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "InsightRep — AI Google Reviews for Indian Hospitality",
  description:
    "Turn happy diners into authentic Google reviews. QR-powered flow for restaurants, cafés, and hotels across India.",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-navy font-sans text-foreground">
        {children}
      </body>
    </html>
  );
}
